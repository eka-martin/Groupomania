import React from "react";
import {AppBar, Toolbar, Typography} from '@mui/material';

function Header() {
    return (
        <AppBar>
            <Toolbar>
                <Typography>
                    GROUPOMANIA
                </Typography>
            </Toolbar>
        </AppBar>
        
                    
    )
}

export default Header;